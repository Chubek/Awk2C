; TODO
(executable
 (name awk2c)
 (public_name awk2c)
 (libraries 
   awk2c.lib
   fmt
   language-c
   containers
   ocamlgraph
   mustache) ; libraries are temporary, will add/remove
 (modes native)
 (preprocess (pps cppo))
 (modules semant codegen main))

; TODO
(rule
  (target awk_runtime.o)
  (deps ../runtime/awk_runtime.c ../runtime/awk_runtime.h)
  (action (run cc -c -o %{targets} %{deps}))
